[phases.setup]
nixPkgs = ["python311Full"]

[phases.install]
dependsOn = ["setup"]
cmds = ["cd Files/backend && python -m pip install -r requirements.txt"]

[start]
cmd = "cd Files/backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
