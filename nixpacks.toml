[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
dependsOn = ["setup"]
cmds = ["cd Files/backend && pip install -r requirements.txt"]

[start]
cmd = "cd Files/backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
