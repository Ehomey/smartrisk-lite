[build]
builder = "NIXPACKS"
buildCommand = "cd Files/backend && pip install -r requirements.txt"

[deploy]
startCommand = "cd Files/backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
